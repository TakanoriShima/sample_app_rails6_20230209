<h2>タイトル</h2>

<!-- 以下の1行を追加 -->
<%= flash[:notice] %>

<p><%= @list.title %></p>
<h2>本文</h2>
<p><%= @list.body %></p>

<h2>画像</h2>
<% if @list.image.attached? %>
  <%= image_tag @list.image, size: "300x300" %>
<% else %>
  <%= image_tag 'sample.png', size: "300x300" %>
<% end %>

<%= link_to "編集", edit_todolist_path(@list.id) %>

<!-- 下記1行を追加してください -->
<%= link_to "削除", destroy_todolist_path(@list.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>